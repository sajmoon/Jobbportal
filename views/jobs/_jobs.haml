%table.table.table-striped
  %thead
    %tr
      %th Företag
      %th Titel
      %th Beskrivning
      %th Tillagd
      %th Kategorier
      %th
  %tbody
    - jobs.each do |job|
      = haml :"jobs/_job", :locals => { :job => job }
