- unless errors.empty?
  .alert
    %button.close{ data: { dismiss: "alert" } }
      x
    .errors
      %strong
        Valideringsfel!
      %ul
        - errors.each do |e, msg|
          %li
            = e
